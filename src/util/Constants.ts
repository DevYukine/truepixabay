/**
 * The base URL from the api used for Image request.
 * @export
 * @constant
 */
export const baseURL = 'https://pixabay.com/api/';

/**
 * The base URL from the api used for Video request.
 * @export
 * @constant
 */
export const videoURL = `${baseURL}videos/`;
